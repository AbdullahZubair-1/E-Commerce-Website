# Which storefront this deployment is for. Every request carries this as the
# X-Site-Slug header, so the backend/chatbot/voice-agent only ever show this
# site's own products and accounts. For the second website, copy this whole
# frontend folder and just change this one value.
VITE_SITE_SLUG=chemisto
